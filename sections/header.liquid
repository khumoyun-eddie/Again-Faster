{{ 'style.header.css' | asset_url | stylesheet_tag }}
<div class="container">
  <header class="header">
    <nav class="navigation">
      {% # theme-check-disable %}
      {% if template == 'index' %}
        <img
          class="navigation__logo"
          src="{{section.settings.logo-light.src | img_url: '240x'}}"
          alt="{{section.settings.logo-light.alt}}"
        >
      {% else %}
        <img
          class="navigation__logo"
          src="{{section.settings.logo-dark.src | img_url: '240x'}}"
          alt="{{section.settings.logo-dark.alt}}"
        >
      {% endif %}
      {% # theme-check-enable %}
      <ul class="navigation__list">
        {% for link in linklists['again-faster-home-menu'].links %}
          <li class="navigation__item">
            <a class="navigation__link" href="{{ link.url }}">{{ link.title }}</a>
          </li>
        {% endfor %}
      </ul>
      <div class="navigation__user">
        {% render 'icon-search' %}
        {% render 'icon-avatar' %}
        {% render 'icon-cart' %}
      </div>
    </nav>
  </header>
</div>

{% schema %}
{
  "name": "Header",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo-light",
      "label": "Logo Light"
    },
    {
      "type": "image_picker",
      "id": "logo-dark",
      "label": "Logo Dark"
    }
  ],
  "blocks": []
}
{% endschema %}
